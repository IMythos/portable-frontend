const LoginPage = () => {
    return (
        <main className="w-full h-screen flex items-center justify-center">
            <form className="w-96 h-2/4 border">
                <div>
                    <h1>Login</h1>
                    <span>Ingresa tus credenciales para acceder al sistema.</span>
                </div>
            </form>
        </main>
    );
}

export default LoginPage;